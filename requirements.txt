# Core Agent & LLM Interaction
# Powers the agentic logic and connects to various language models.
smolagents==1.22.0
litellm==1.74.0

# Vector Database & Embeddings
# Used for storing and retrieving document chunks (embeddings).
chromadb==1.0.13
sentence-transformers==4.1.0

# PDF Parsing & Text Processing
# Tools for extracting text, splitting it, and handling PDF files.
PyMuPDF==1.26.1
pymupdf4llm==0.0.25
pikepdf==9.10.2
langchain-text-splitters==0.3.8

# Data Handling & Structuring
# For working with dataframes and serialization.
pandas==2.3.0
pyarrow==21.0.0

# Natural Language Processing (NLP)
# Used for Named Entity Recognition (NER) to extract authors, etc.
spacy==3.8.7
# After installing, download the model: python -m spacy download en_core_web_sm

# User Interface
# Creates the Gradio web interface for the chatbot.
gradio==5.38.0

# Utilities
# Miscellaneous packages for environment variables, HTTP requests, configuration, and progress bars.
python-dotenv==1.1.1
requests==2.32.4
PyYAML==6.0.2
tqdm==4.67.1
